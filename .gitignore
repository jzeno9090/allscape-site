# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Squarespace scrape archive — local-only image/video/HTML library.
# Kept out of git to avoid 100MB+ repo bloat. Pull files into public/images/
# (root) to deploy individual assets.
/scraped/
/public/images/scraped/

# Claude Code session tooling (preview server config, etc.)
/.claude/
